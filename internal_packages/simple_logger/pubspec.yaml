name: simple_logger
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  cli_util: ^0.4.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
